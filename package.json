{
  "dependencies": {
    "tree-sitter-cli": "^0.20.8"
  },
  "scripts": {
    "tree-sitter": "tree-sitter",
    "generate": "tree-sitter generate",
    "build-wasm": "tree-sitter build-wasm",
    "playground": "yarn generate && yarn build-wasm && yarn playground"
  },
  "devDependencies": {
    "nan": "^2.17.0"
  },
  "main": "bindings/node"
}
